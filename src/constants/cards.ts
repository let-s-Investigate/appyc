import { radius } from '@/theme/radius';
import { shadows } from '@/theme/shadows';

export const Cards = {
  radius: radius.md,
  padding: 16,
  shadow: shadows.card,
  borderWidth: 1,
} as const;

export default Cards;
